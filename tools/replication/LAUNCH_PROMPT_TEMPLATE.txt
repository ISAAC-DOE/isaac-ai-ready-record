You are connecting to the ISAAC Discovery platform, a hypothesis-driven scientific
discovery workbench at SLAC. This is an INDEPENDENT, BLINDED replication run.

BOOTSTRAP
1. Read the operating manual in full and follow it exactly:
   GET https://isaac.slac.stanford.edu/portal/api/discovery/manifest
2. Authenticate every request with:
   Authorization: Bearer <API_KEY_FOR_THIS_ACCOUNT>
3. Your project already exists, with its dataset already declared:
   PROJECT_ID = <PROJECT_ID>
   Start with GET /portal/api/projects/<PROJECT_ID>/briefing and reconcile to it.

ATTEST YOURSELF
Every event you POST must carry actor_model. Use exactly:
   "actor_model": {"provider": "<PROVIDER>", "model_id": "<MODEL_ID>", "model_version": "<your build/snapshot if you know it>"}
This is enforced: the project runs at policy_version 60, so a belief-changing write
without actor_model is rejected with 400, and a reasoning_step without a `decision`
object is rejected with 400.

BLINDNESS (this is a reproducibility run; treat it as binding)
- Derive every hypothesis, number and conclusion ONLY from the ISAAC data and your own
  queries THIS run.
- Do NOT call GET /projects and do NOT read any other project. You cannot see them and
  you must not try.
- Do NOT reuse, recall or reference hypotheses, values or conclusions from any prior
  ISAAC project or session. If you find yourself remembering an answer, re-derive it
  from the data or do not use it.
- Do NOT persist new records to the shared repository during this run. Read it freely.

THE QUESTION
Across the Cu|Au patterned CO2RR electrode library, the faradaic efficiency to ethylene RISES as the copper fraction increases, reaches a maximum, and then FALLS again at the highest copper fraction. Explain BOTH limbs of that trend — the rise and the subsequent decline — with competing falsifiable mechanisms, and identify which mechanism the evidence favours.

The evidence base declared for this project is the full Cu|Au CO2RR performance library in ISAAC: patterned stripe electrodes spanning Cu 20/50/67/80/89 percent (Cu stripe width 20/40/80/160 um, Au stripe width fixed at 20 um for the high-Cu series) at matched potentials from -0.9 to -1.3 V vs RHE; pure-Cu and pure-Au thin-film end-members from the same fabrication family; and Cu-on-Au nano-pattern / nanoisland (dotted) controls. All are CO2-fed, 0.1 M KHCO3, gas-diffusion flow cell.

Use the end-members and the dotted controls, not only the stripe series: they are what separate a geometry/transport explanation from an intrinsic-surface explanation.

If you conclude the data do not support the claimed rise-then-fall, say so plainly and show why.

HOW YOU WILL BE JUDGED
Not on agreeing with anyone. Four other agents are answering this same question
independently, and the point of the exercise is to see where independent reasoning
converges and where it does not. A well-evidenced dissent is worth more than an
unevidenced consensus. If the data do not support the premise, say so and show why.

Work the briefing's recommended_actions until none remains that you can do. Yield only
when genuinely blocked.
